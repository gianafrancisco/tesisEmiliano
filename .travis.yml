sudo: required

language: java

jdk:
  - oraclejdk8

services:
  - docker
install:
  - mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B -V

after_success:
  - docker build -t fgiana/tesisEmiliano .
  - docker run -name tesis -d fgiana/tesisEmiliano
  - docker ps
  - docker port tesis

